version: '3.8'

services:
  coto:
    build: .
    volumes:
      - ./input:/data/input
      - ./output:/data/output
    command: -i /data/input -o /data/output/combined.txt --format json --verbose
    
  coto-cli:
    image: yourusername/coto:latest
    volumes:
      - ${PWD}:/data
    command: -i /data -o /data/output.txt --ext .go,.md --parallel 4