project:
  name: coto
  description: Coto CLI - File combination utility
  longDescription: |
    A powerful CLI tool to recursively combine file contents from directories and subdirectories.
  links:
    homepage: https://github.com/bhangun/coto
    bugTracker: https://github.com/bhangun/coto/issues
    help: https://github.com/bhangun/coto/blob/main/README.md
  authors:
    - Bhangun
  license: MIT
  inceptionYear: 2024
  tags:
    - cli
    - go
    - golang
    - utility
    - tool
    - file-combiner

release:
  github:
    owner: bhangun
    name: coto
    overwrite: true
    prerelease:
      enabled: false
    update:
      enabled: true
      sections:
        - BODY
        - ASSETS

distributions:
  coto:
    type: BINARY
    executable:
      name: coto
    artifacts:
      - path: ./dist/linux-amd64/coto
        platform: 'linux-x86_64'
        extraProperties:
          packager: brew,chocolatey
        transform: './dist/linux-amd64/coto -> {{distributionName}}'
      - path: ./dist/linux-arm64/coto
        platform: 'linux-aarch_64'
        extraProperties:
          packager: brew,chocolatey
        transform: './dist/linux-arm64/coto -> {{distributionName}}'
      - path: ./dist/darwin-amd64/coto
        platform: 'osx-x86_64'
        extraProperties:
          packager: brew,chocolatey
        transform: './dist/darwin-amd64/coto -> {{distributionName}}'
      - path: ./dist/darwin-arm64/coto
        platform: 'osx-aarch_64'
        extraProperties:
          packager: brew,chocolatey
        transform: './dist/darwin-arm64/coto -> {{distributionName}}'
      - path: ./dist/windows-amd64/coto.exe
        platform: 'windows-x86_64'
        extraProperties:
          packager: brew,chocolatey
        transform: './dist/windows-amd64/coto.exe -> {{distributionName}}.exe'
      - path: ./dist/windows-arm64/coto.exe
        platform: 'windows-aarch_64'
        extraProperties:
          packager: brew,chocolatey
        transform: './dist/windows-arm64/coto.exe -> {{distributionName}}.exe'

signing:
  active: NEVER
  verify: false
  pgp:
    armored: true

packagers:
  brew:
    active: RELEASE
    commitAuthor:
      name: bhangun
      email: n90d1n9@gmail.com
    repository:
      active: RELEASE
      owner: bhangun
      name: homebrew-coto
      branch: main
    formulaName: coto
    skipTemplates: []

  chocolatey:
    active: RELEASE
    commitAuthor:
      name: bhangun
      email: n90d1n9@gmail.com
    repository:
      active: RELEASE
      owner: bhangun
      name: coto-chocolatey
      branch: main
    packageName: coto
